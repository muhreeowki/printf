#include "main.h"

void _print_integer(int n)
{
    
    if (n < 0)
    {
        _putchar('-');
        n = -n;
    }

    
    if (n / 10 != 0)
    {
        _print_integer(n / 10);
    }

    _putchar('0' + n % 10);
}

